spring:
  datasource:
    username: newstream
    password: newstream
    url: jdbc:mysql://localhost:3306/newstream

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  sql:
    init:
      mode: always
